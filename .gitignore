# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/

# uv
.uv/

# Tooling caches
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Environment / secrets -- never commit real credentials.
# The example file is the one exception and must stay tracked.
.env
.env.*
!.env.example

# Rollout / training artifacts
runs/
outputs/
*.jsonl
!tests/**/*.jsonl
checkpoints/
wandb/

# OS
.DS_Store

# Experiment run artifacts are large and reproducible from the manifest.
# Reported runs are committed deliberately with `git add -f`.
runs/
.env
